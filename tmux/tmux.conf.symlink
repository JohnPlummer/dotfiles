# Set theme to Nord
run-shell "~/.dotfiles/tmux/nord-tmux/nord.tmux"

set -g default-terminal 'screen-256color'
set -ga terminal-overrides ',*256col*:Tc'

# rebind prefix to C-a
unbind C-b
set -g prefix C-a
bind C-a send-prefix
